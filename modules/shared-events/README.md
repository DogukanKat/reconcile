# shared-events

Avro schemas for cross-service events.

Empty until Phase 3, when Avro and the Confluent Schema Registry arrive.
Phase 1 uses JSON event payloads embedded in the outbox table directly;
nothing is shared across modules yet.
